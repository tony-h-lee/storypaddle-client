// import React from 'react';
// import { shallow } from 'enzyme';

// import SettingsPageComponent from '../index';

describe('<SettingsPageComponent />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
