// import React from 'react';
// import { shallow } from 'enzyme';

// import NarrativeOverviewNotFound from '../index';

describe('<NarrativeOverviewNotFound />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
