// import { fromJS } from 'immutable';
// import { selectNarrativeOverviewPageDomain } from '../selectors';

describe('selectNarrativeOverviewPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
