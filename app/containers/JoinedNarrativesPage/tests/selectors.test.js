// import { fromJS } from 'immutable';
// import { selectJoinedNarrativesPageDomain } from '../selectors';

describe('selectJoinedNarrativesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
