// import { fromJS } from 'immutable';
// import { selectCreateNarrativesPageDomain } from '../selectors';

describe('selectCreateNarrativesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
